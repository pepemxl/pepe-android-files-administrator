<?xml version="1.0" encoding="utf-8"?>
<!--
  Cleartext HTTP is denied by default. We allow it ONLY for local development
  hosts, so the app can reach services running on the dev machine over http://:
    - 10.0.2.2  : the host machine as seen from the Android emulator
    - localhost : the device itself (e.g. adb reverse tunnels)
  Used for local MinIO (S3 endpoint http://10.0.2.2:9250) and the light P2P
  orchestrator over http polling. Production remotes stay HTTPS-only.
-->
<network-security-config>
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="true">10.0.2.2</domain>
        <domain includeSubdomains="true">localhost</domain>
        <domain includeSubdomains="true">127.0.0.1</domain>
    </domain-config>
</network-security-config>
